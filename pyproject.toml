[project]
name = "awesome-agent-sandboxes"
version = "0.1.0"
description = "Comprehensive guide to sandboxing options for AI agents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.33.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
